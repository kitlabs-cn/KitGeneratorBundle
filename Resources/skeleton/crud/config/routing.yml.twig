{{ route_name_prefix }}_homepage:
    path:     /
    defaults: { _controller: "{{ bundle }}:{{ entity }}:index" }
    options:
        menu:
            name: "{{ route_name_prefix }}列表" # 路由中文名称
            icon: "" #路由小图标，无则留空
            status: 1 #路由状态，1启用，0禁用
            is_show: 1 #是否显示，1显示，0不显示
            is_authorize: 1 #是否需要授权访问，1授权才能访问，0无须授权（即不需要配置该路由就能进行访问）
            level: 2 #路由级别
            parent_route: {{ extension_alias }}_homepage #上一级路由名称

{% if 'index' in actions %}
{{ route_name_prefix }}_index:
    path:     /index
    defaults: { _controller: "{{ bundle }}:{{ entity }}:index" }
    options:
        menu:
            name: "{{ route_name_prefix }}列表" # 路由中文名称
            icon: "" #路由小图标，无则留空
            status: 1 #路由状态，1启用，0禁用
            is_show: 1 #是否显示，1显示，0不显示
            is_authorize: 1 #是否需要授权访问，1授权才能访问，0无须授权（即不需要配置该路由就能进行访问）
            level: 3 #路由级别
            parent_route: {{ route_name_prefix }}_homepage #上一级路由名称
{% endif %}

{% if 'show' in actions %}
{{ route_name_prefix }}_show:
    path:     /show
    defaults: { _controller: "{{ bundle }}:{{ entity }}:show" }
    options:
        menu:
            name: "{{ route_name_prefix }}列表" # 路由中文名称
            icon: "" #路由小图标，无则留空
            status: 1 #路由状态，1启用，0禁用
            is_show: 1 #是否显示，1显示，0不显示
            is_authorize: 1 #是否需要授权访问，1授权才能访问，0无须授权（即不需要配置该路由就能进行访问）
            level: 3 #路由级别
            parent_route: {{ route_name_prefix }}_homepage #上一级路由名称
{% endif %}

{% if 'new' in actions %}
{{ route_name_prefix }}_new:
    path:     /new
    defaults: { _controller: "{{ bundle }}:{{ entity }}:new" }
    options:
        menu:
            name: "{{ route_name_prefix }}新增" # 路由中文名称
            icon: "" #路由小图标，无则留空
            status: 1 #路由状态，1启用，0禁用
            is_show: 1 #是否显示，1显示，0不显示
            is_authorize: 1 #是否需要授权访问，1授权才能访问，0无须授权（即不需要配置该路由就能进行访问）
            level: 3 #路由级别
            parent_route: {{ route_name_prefix }}_homepage #上一级路由名称
{% endif %}

{% if 'edit' in actions %}
{{ route_name_prefix }}_edit:
    path:     /edit
    defaults: { _controller: "{{ bundle }}:{{ entity }}:edit" }
    options:
        menu:
            name: "{{ route_name_prefix }}编辑" # 路由中文名称
            icon: "" #路由小图标，无则留空
            status: 1 #路由状态，1启用，0禁用
            is_show: 1 #是否显示，1显示，0不显示
            is_authorize: 1 #是否需要授权访问，1授权才能访问，0无须授权（即不需要配置该路由就能进行访问）
            level: 3 #路由级别
            parent_route: {{ route_name_prefix }}_homepage #上一级路由名称
{% endif %}

{% if 'delete' in actions %}
{{ route_name_prefix }}_delete:
    path:     /delete
    defaults: { _controller: "{{ bundle }}:{{ entity }}:delete" }
    options:
        menu:
            name: "{{ route_name_prefix }}删除" # 路由中文名称
            icon: "" #路由小图标，无则留空
            status: 1 #路由状态，1启用，0禁用
            is_show: 1 #是否显示，1显示，0不显示
            is_authorize: 1 #是否需要授权访问，1授权才能访问，0无须授权（即不需要配置该路由就能进行访问）
            level: 3 #路由级别
            parent_route: {{ route_name_prefix }}_homepage #上一级路由名称
{% endif %}
